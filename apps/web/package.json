{
  "name": "@maos/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "dependencies": {
    "@maos/module-operations": "0.0.0"
  },
  "scripts": {
    "prebuild": "npm run build --workspace @maos/logging && npm run build --workspace @maos/module-operations",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js"
  }
}
